import React from 'react'
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native'

export default FlashCard = (props) => {
  return (
    <View style={styles.flashCard}>
    </View>
  )
}

const styles = StyleSheet.create({
  flashCard: {
  }
})